public class Fahrrad extends Fahrzeug {

    public Fahrrad(int anzahlRaeder, int speed, double maxSpeed, double position) {
        super(anzahlRaeder, speed, maxSpeed, position);
    }

    @Override
    public double getMaxSpeed() {
        return super.getMaxSpeed();
    }
}
